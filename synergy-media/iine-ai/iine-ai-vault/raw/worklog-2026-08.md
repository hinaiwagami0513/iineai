# raw: 2026年8月 作業ログ

> 生素材。**新しい日を上に足す**。既に書いた日のセクションは書き換えない（間違いは同じ日の下に訂正を足す）。
> このリポジトリは公開なので、**報酬・請求・契約形態と社外秘の議題は書かない**（[[raw/mtg-2026-08-21-iwagami]] と同じ方針）。
> 取引先への共有はこのファイルを原本にする。1日分は「やったこと / 見つけた不具合 / 次にやること」の3節で揃える。
> 収集コマンド: `tools/collect-worklog.sh 2026-08-22`（その日のコミットと差分統計を出す）

---

## 2026-08-27（木）

### やったこと

**投稿編集（エディタ面）のスマホ表示を直す — ワイヤー8本 + 共通CSS 1本**
- 幅390pxでフロー4種と投稿編集の全ステップ44面、編集タブ14面、オーバーレイ22面を機械計測し、「親の `overflow:hidden` で切り落とされている要素」と「テキストが1行3文字以下に潰れている要素」を洗い出した。崩れは全部「投稿を編集」の面に集まっていて、前段のステップ（SNS選択・作り方・素材・一括確認）は既に無事だった
- 根本は3カラム（左240px プレビュー / 中央1fr 台本・投稿文 / 右auto AIパネル）が390pxで畳まれていないこと。`flow_video_script` と `flow_manual` にはこの畳みルールが無く、中央カラムが36pxまで潰れて編集タブと台本テーブルが親に切り落とされ、AIパネルは280pxのまま画面外（right=580px）に出ていた
- インラインstyleに依存した `[style*="grid-template-columns:240px"]` の当て方をやめ、`.ed-head` / `.ed-cols` / `.ed-tabs` / `.ed-panehd` / `.ed-typerow` のクラスを6ファイルに付与。SP対応は `editor-sp.css` 1本に集約し、各ファイルに散っていた重複ルールを消した（`shell-sp.css` / `topbar-sp.css` と同じ「レイアウト型ごとに1ファイル」の形）
- ヘッダー行はタイトル左のSNS種別アイコンをSPで外して、タイトルに1行を丸ごと渡した。「仮予約」14px・「自動保存ずみ」44pxで縦一列になっていたのが、仮予約（左）＋自動保存ずみ（右）の1行に収まる。種別はトップバーのタグとステッパーで分かる
- 台本テーブルは列に下限（96 / 210 / 150px）を与えて横スクロールにした（`shell-sp.css` の「表は列を潰さず横スクロール」と同じ判断）。編集タブは左右の余白を20→14pxに詰めて3つを336pxに収めた
- 仮予約ダイアログのフッターは4ボタンで350pxを分け合って「や / め / る」になっていた。「未定に戻す」に1行、「やめる・仮予約する・本予約する」に1行を渡し、仮と本を並べて比較できる形にした

**動画エディタ（ダーク）のSPが実質使えていなかったのを直す**
- `.tools`（ツールレール）と `.left-props`（プロパティ）が `position:absolute` のまま幅だけ100%にしてあり、ステージとタイムラインの上に重なっていた。プレビューもトラックも見えず黒い空白が出るだけの状態。position から降ろしてDOM順（ツール → プロパティ → ステージ → AI → タイムライン）に縦積みした
- タイムラインの目盛りは36秒を幅いっぱいに割る作りで、390pxだと2秒ごとの間隔が18pxになり「10s」以降（幅22px）が隣と重なって読めない。`.ruler .mk:nth-child(even)` を隠して4秒ごとにした（位置はJSがpxで持つので触らない）
- トラック表示が102pxで6本のうち2本しか見えなかったので `.tl` を180→240pxにして3本ぶん見せる
- パネル幅を引っぱるグリップ（`.props-resize` / `.resize-handle`）は1カラムでは掴む先が無いのに画面外（x=397）に残っていたので隠した

**画像エディタ（ダーク）と素材アップロード面の細部**
- ヘッダーの作品名を省略表示（`flex:1` + ellipsis）にして「保存して投稿へ」を1行に。`video_editor_dark` で既にやっていた判断を揃えた
- 「この画像の台本 / 1 / 4枚目 / タップで開く」は `white-space:nowrap` が無く見出しだけが min-content まで縮んで「この画像の台 / 本」に割れていた。3つ合わせて325pxなので nowrap で1行に収まる
- 素材の入口タブ「PCからアップロード / いいねドライブから選択」が padding 20px + body で2行に割れていたので余白と字を詰めた（`flow_image_material` / `flow_video_material`）
- ラベルピル `.tag2`「おすすめ」が行の余りを分け合って42pxまで縮んでいたので縮ませないようにした（`post_editor_image`）

### 見つけた不具合

- `post_editor_wire.html`（自動保存ずみ・シーン構成が縦積み）と `base_chat2_wire.html`（トップバーのタグが2行）にも同じ崩れが残っている。どちらも `board.html` に登録が無い旧版なので触っていない（現行は `post_editor_image_wire` と `chat_wire`）
- `video_editor_dark` の変化ポイント（`.kf`）は親クリップの下端から7px出て切れる。PC幅でも同じで `bottom:-2px` の設計どおりなので直していない
- 検出を組む途中で2回誤った判断をした。①要素の高さ ÷ line-height で行数を数えるとボタンの上下 padding を1行と誤って数える（Range の `getClientRects()` で実行数を数える形に直した）。②dev-server が落ちた状態で計測すると全ページが `chrome-error` になり「崩れ0」に見える（結果にページタイトルと要素数を含めて検証できるようにした）

### 次にやること

- 計測したのは幅390px（iPhone 14 Pro相当）だけ。360px（Android小型）と430pxの境界そのものは確認していない
- 崩れの検出スクリプトは使い捨てで作った。繰り返し測るなら `tools/` に置く形にしたい

---

## 2026-08-24（月）

### やったこと

**TikTokのブランド色を茶からティールに変更（29ファイル）**
- 旧値 `sns-tiktok #854f0b` が `warning #ad6201`、`sns-tiktok-subtle #fef3c7` が `warning-border #ffe6c4` と近く、TikTokの行が「警告」に見えていた。TikTokのブランド色（黒／シアン `#25F4EE`／マゼンタ `#FE2C55`）に黄系は無く、他3つがブランド由来なのにTikTokだけ無関係な色だった
- 新値 `#0a6f66` / subtle `#e5fbf7`。色相174.7°で `info`(212°) から37.3°・`success`(135.7°) から39°離し、subtle地のコントラスト5.60:1（既存3色の4.79〜6.04の帯に収まる）／白文字時6.04:1
- ワイヤー27本 + `DESIGN.md` + `help_site/app/globals.css` を一括更新。トークン定義行だけを置換したので、`analysis_posts` のサムネのグラデーションや `analysis_visual` の銅メダル `#fef3c7` は残っている

**カードの中の説明文・使い方・見本を落とす**
- `settings_wire` の運用目的から進み具合の案内・使い方ピル4個・資料カードの説明・「書き方の例」を削除（11箇所）。空状態は「まだ書いていません」だけに。目標ダイアログの週1/3/5/7本の説明4個も削除（本数は数字で自明）
- SNS設定から説明文18箇所、`request_wire` の発注相談／出張撮影から POINT・選択肢・ステップの補足18箇所、`board.html` のギャラリー注記を削除。選択肢を見分ける一文（言葉づかいの口調例・撮影レベルの説明）とデータ・ラベル・状態表示は残した
- `DESIGN.md` の §Writing に原則4「カードの中に説明文と使い方を置かない」を追加。補助説明を「噛み砕いてよい（1〜2行）」→「カードの中には置かない」に変更し、Do's の「説明・ヒントのチップは primary-subtle + primary」を失効扱いに（記録・推し情報のチップだけオレンジで残す）
- `iine-ai-vault/concepts/no-in-card-help-text.md` を新規作成し `INDEX.md` のデザイン系に登録

**SNS設定に投稿ルール4項目を追加**
- 実アプリの投稿ルール5項目のうちワイヤーには「使ってほしくない言葉」しか無かったため、推奨ワード・具体ルール・投稿言語・撮影レベルを既存の言葉づかいと同じ表示／編集切替（`.pj`）で追加
- 4項目を毎回入れ直さずに済むよう、カードの手前に「ほかのSNSから写す」を設置。候補は一覧の連携済みの行から作り、現在のSNSと未連携は出さない。上書きなので実行前に変更内容を提示し、写すのはルール5項目だけ（出し方・本数の目標・承認は含めない）。`closeRowMenus` が `.iconbtn` だけを見ていたので `.act` 配下すべてに広げた

**設定画面のカードを2列等幅に揃える**
- 3列に2/1で混ぜる弁当組みがルール追加で8枚になり幅727/355・高さ4種に散っていたため2列に統一。`repeat(2,1fr)` では429/653に割れる（1fr の最小値が auto で min-content に押し広げられる）ので `minmax(0,1fr)` で厳密に等幅にし、高さの近いカードを隣に置く並び順にした
- 見出し8枚に丸チップのアイコンを追加（色は全部 primary）。`.pj`(16px/左右18px/罫線なし) と `.card`(20px/左右20px/罫線あり) でずれていた3点を `.pj` 側に寄せ、本文の左右余白も18pxに統一（`.bento` の中だけ）
- チップを持つ見出しは中央揃えに（`.pj-hd` が `align-items:flex-start` でタイトルがチップ中心より9px上にずれていた）

**作業ログの素材収集をワークスペース対応に**
- `collect-worklog.sh` の収集先を、メインリポジトリ1箇所から「メイン + ネストしたリポジトリ + その linked worktree」に拡大。名前と場所が作り直すたびに変わるので `.git` の有無で毎回探し、コミット取得は `--all` にした
- ネストしたリポジトリはメイン側で pathspec 除外して二重計上を防止。`.gstack` / `.superset` / `.DS_Store` は除外し、セクションは中身があるときだけ本体に足す

### 直した不具合
- 見通しカードが541pxに縮み、グラフに残る幅263pxに対し12ヶ月の月ラベルが nowrap で約360px必要だったため、`.bar-chart` が枠から溢れてタイルの下に潜り月が読めなかった。グラフとタイルを縦積みにしてグラフに全幅を渡し、タイルは3列に寝かせた
- 見出しの「週3本で続けた場合の目安」を削除（すぐ下の key タイル「1週間に出す本数 / 3本」と同じことを言っている）。`goalSave` の `fcPerWeek` への代入も外した
- `settings_wire` で `ti-chevron-left` が未宣言のため「SNS設定にもどる」の矢印がずっと出ていなかった。`ti-copy` と `ti-info-circle` は同梱フォントに glyph が無いので `clipboard-list` / `help` に差し替え、撮影レベル用に `ti-photo` を追加
- 説明文が消えて1行になった選択肢カードが上揃えで文字がアイコン中心より上にずれていたので、価格行を持たないカードは中央揃えに
- `daily-worklog.sh` が「（この日のコミットなし）」等の日本語文言を grep して判定していたため、素材の書式を変えた時点で毎日空のログを書く状態に静かに倒れていた。`collect-worklog.sh` が素材ゼロの日に `<!-- WORKLOG:NO-MATERIAL -->` を出し、それを見て打ち切る方式に変更

### 次にやること
- 撮影レベルは文章ではなく画像・動画の提案内容を変えるもので他のルールと役割が違う。いまは投稿ルールに並べたままなので置き場所の再検討（コードにコメントで申し送り済み）

## 2026-08-22（土）

※ この日ぶんは未コミット。次のコミットで確定する。

### やったこと

**「台本から」のアイコンを巻物に変更（全画面）**
- ビデオカメラ（`ti-video`）とキラキラ（`ti-sparkles`）で画面ごとに割れていたのを、巻物1種に統一
- 対象5ファイル: `flow_video_script` / `flow_video_material` / `flow_image_v3` / `flow_image_material` / `post_editor_image`
- ワイヤーに埋め込んだ Tabler サブセットフォントに巻物（`ti-script`）が入っていないため、24グリッド・`stroke-width:2`・round cap を合わせたインライン SVG で自作
- `DESIGN.md` に「アイコンは全部 Tabler、自作は台本の巻物だけ」を明記

**選択カードのアイコンを丸チップ化（全画面）**
- 作成方法カード（画像／動画／完成品）と台本・素材カードのアイコンを 64px の丸ベタ + アイコン32px に変更
- 選択中 = `primary(#fe7235)` 塗り + 白アイコン、未選択 = `border(#efe7df)` 塗り + `muted-foreground(#757575)` アイコン
- 対象8ファイル（上記5つ + `flow_image_v2` / `flow_video_v2` / `flow_manual`）
- 当初 `primary-deep(#d54101)` + グレー塗りで組んだが濃すぎたため上記に修正。`DESIGN.md` の「白は大字なら primary 上で許容」の記述に合わせた
- アイコンサイズが v2系だけ25pxだったのを64pxに統一
- `DESIGN.md` に丸チップの規定（色・サイズ・インラインstyleで上書きしない）を追記

**案カード（まとめて作る）の余白修正**
- タグ行の下パディングが `0` で点線区切りにくっついていたのを、点線の上下 12px（spacing `md`）の対称に変更
- 対象2ファイル: `flow_video_script` / `flow_image_v3`

### 見つけた不具合（未修正）

**案カードが表示されない（4ファイル）**
- `<script>` ブロックの先頭が `)();` から始まっており、ブロック全体が SyntaxError で実行されない
- そのため `BULK_SAMPLES` が未定義になり、「◯件の案を見る」を押しても案カードが出ない
- 該当: `flow_image_v3` / `flow_video_material` / `flow_manual` / `flow_image_material`
- `flow_video_script` だけは正常
- ブラウザのコンソールには出ない（パース時エラー）ため、`node --check` で特定
- 8/21 のコミット `2379205` で同種のエラーを1件直しているが、残り4ファイルが未対応だった

### 次にやること
- 上記 SyntaxError 4ファイルの修正
- 作業ログのスプレッドシート連携と毎日の自動生成

---

## 2026-08-21（金）

コミット4件: `b4d03bc` / `7e531c0` / `3348234` / `2379205`
規模: 47ファイル / +4,475行 / −1,898行

### 会議（10:58 岩上さん×いいねAI）で出た要望への対応

| 要望 | 対応 |
|---|---|
| ダッシュボードの「AIらしさ」を抑え色数を減らす | パレット全面刷新 |
| 画像・動画・PDF・Webリンクを1箇所に集約 | いいねドライブの2タブを1つの一覧に統合 |
| 投稿作成フローの「次へ」ボタンを廃止 | 選択した時点で次へ進む方式に変更 |
| 仮予約／本予約をフロー内で選択 | 予約ダイアログで選択可能に。完了メッセージも書き分け |
| SNSプレビューにセーフティゾーンの点線 | 動画（台本）フローのプレビューに追加 |

詳細は [[raw/mtg-2026-08-21-iwagami]]。

### やったこと

**デザイン基盤の刷新**
- メインカラー `#ef6108` → `#fe7235`、背景を生成り `#fcf5ef` に
- 情報・補助用の青 `#0077ff` を新設（押せる要素には使わない運用ルールつき）
- サイドバーを縦グラデーションに変更、選択中・ホバーの表現を再設計
- 画面間で食い違っていた色定義6件を統一。TikTokのブランド色が2種に割れていたのも統一
- グラフの配色を単一色相ランプに再確定
- フォントを Inter + Noto Sans JP の2段構成に（英数字のみ Inter）
- 全33ファイル中25ファイルで Webフォントが読み込まれていなかったため修正
- 文字サイズの下限を14pxに引き上げ（該当449箇所）。40〜50代の管理画面未経験者を想定した判断

**投稿作成フロー**
- 選択肢が1つだけの画面は選ぶだけで次に進む方式へ（「次へ」ボタンを非表示）
- 選んだ作成方法に応じて正しい画面へ分岐するよう修正（「動画を選んだのに画像の枚数を聞かれる」不整合を解消）
- 仮予約／本予約をダイアログ内で選択可能にし、完了メッセージを状態別に書き分け（本予約=「予約ができました」／仮予約=「準備ができました」）
- 予約の入口を1つに統一。旧日時ピッカーを使っていた5画面を新ダイアログに接続
- 動画2フローの完了表示を、通知のみからモーダル表示に変更
- 完成画面で予約が確定済みの場合は「投稿を予約する」を表示しないよう修正

**いいねドライブ**
- 「素材」「AI読み取り資料」の2タブを1つの一覧に統合。種別の違いは詳細パネルの操作で表現
- 新規フォルダ作成・URL追加・アップロードの各ダイアログを実装（従来は押しても反応しない状態）

**設定画面**
- 「言葉づかい」を運用目的から SNS設定へ移設。編集フォームの補足説明を整理
- SNS設定を「一覧 → アカウント詳細」のドリルイン構成に再編
- 運用目標と見通しの画面を新規追加（旧アプリの目標設定に相当する画面が未整備だったため）
- Facebook同時投稿の項目を Instagram 以外では非表示に

### 直した不具合
- 台本のシーン表が表示されない不具合（データと描画処理の欠落を復旧）
- スクリプトの構文エラー1件（コンソールに出ないため `node --check` で特定）
- 画像台本ルートの画面ラベルが「動画（台本）」と誤表示
- 承認済み画面から予約する際、予約種別を選ばずに確定してしまう問題
- 枚数選択画面が自動送りの対象外だった

### ドキュメント
- 8/21 会議録を `raw/` に記録（グループ外の議題は除外）
- `DESIGN.md` と設計ナレッジ10件を新パレット・新方針に追従
